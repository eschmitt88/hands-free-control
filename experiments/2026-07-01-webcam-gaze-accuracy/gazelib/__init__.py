"""gazelib — webcam gaze/head-pose accuracy characterization helpers.

Import boundary (enforced so analysis runs on a headless server):
  - geometry, calibrate, dwell  -> numpy / sklearn / pyyaml / stdlib ONLY.
  - features                    -> may import mediapipe + cv2 (workstation only).
"""
